package io.github.RashRogues;

public enum EntityAlignment {
    PLAYER,
    ENEMY,
    // if UI ends up being not an entity, then we can remove this.
    // otherwise, we could rename it to UI or Background or something
    X
}
